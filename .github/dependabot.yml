version: 2
updates:
  # Enable version updates for npm
  - package-ecosystem: "npm"
    # Look for a package.json in the root directory
    directory: "/"
    # Check for library updates once a week
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10
